<!DOCTYPE html>
<html data-theme="emerald" data-turbo-prefetch="false">
  <head>
    <title><%= content_for(:title) || "Manuscript Development by RocketMoon" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="devise-auth flex items-center justify-center bg-gray-200 min-h-screen h-full py-8">
    <div class="flex flex-col rounded-2xl px-12 py-8 bg-white shadow-lg w-[28rem]">
      <div class="mb-4 flex items-center justify-center">
        <%= image_tag "manuscript-rocketmoon-logo.png", class: "w-64" %>
      </div>
      <%= yield %>
      <div class="text-center mt-4">&copy; <%= Time.now.year %> Dr. Khushboo Vaishnav &amp; Kulbir Saini</div>
    </div>
  </body>
</html>
